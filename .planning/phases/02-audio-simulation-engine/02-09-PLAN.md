---
phase: 02-audio-simulation-engine
plan: 09
type: execute
wave: 9
depends_on: [02-08]
files_modified:
  - src/audio/renderSimulation.ts
  - src/audio/renderTypes.ts
  - src/store/audioStore.ts
  - src/ui/SimulationDiagnostics.tsx
  - src/ui/SimulationPanel.tsx
autonomous: false
gap_closure: true
must_haves:
  truths:
    - "Render diagnostics report elapsed time for each render"
    - "Official corpus results are visible in the diagnostics panel"
    - "Human verification can confirm performance and corpus status"
  artifacts:
    - path: "src/audio/renderSimulation.ts"
      provides: "Render timing capture"
    - path: "src/audio/renderTypes.ts"
      provides: "Render timing metadata"
    - path: "src/ui/SimulationDiagnostics.tsx"
      provides: "Corpus + performance status display"
  key_links:
    - from: "src/audio/renderSimulation.ts"
      to: "src/store/audioStore.ts"
      via: "render result state update"
      pattern: "render"
    - from: "src/ui/SimulationDiagnostics.tsx"
      to: "src/store/audioStore.ts"
      via: "diagnostics display"
      pattern: "corpus|render"
---

<objective>
Instrument render performance and confirm corpus validation results in the UI, then run human verification checks.

Purpose: Close remaining Phase 2 gaps tied to performance and official corpus verification.
Output: Render timing metrics surfaced in diagnostics plus a human verification checkpoint.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-audio-simulation-engine/02-VERIFICATION.md
@.planning/phases/02-audio-simulation-engine/02-06-SUMMARY.md
@src/audio/renderSimulation.ts
@src/ui/SimulationDiagnostics.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture render timing metrics and persist in state</name>
  <files>src/audio/renderSimulation.ts, src/audio/renderTypes.ts, src/store/audioStore.ts</files>
  <action>
    Measure render duration using performance.now around the renderSimulation execution
    and include elapsed milliseconds in the render result type. Persist the timing
    metadata in audioStore alongside the latest render output so the UI can display it.
    Keep existing render warnings/progress handling intact.
  </action>
  <verify>npm run typecheck</verify>
  <done>Each render result includes an elapsed time value in state.</done>
</task>

<task type="auto">
  <name>Task 2: Show render timing and corpus status in diagnostics UI</name>
  <files>src/ui/SimulationDiagnostics.tsx, src/ui/SimulationPanel.tsx</files>
  <action>
    Display the latest render timing (ms) and corpus pass rate in SimulationDiagnostics.
    Ensure corpus results are visible with a clear last-run indicator, and wire any
    missing UI actions so users can trigger the official corpus run from the panel.
    Keep layout consistent with existing diagnostics styling.
  </action>
  <verify>npm run typecheck</verify>
  <done>Diagnostics panel shows render timing and corpus results after a run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Render timing instrumentation and corpus diagnostics display.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the Simulation panel.
    2. Render a 30-second demo (e.g., basic-delay.spn with guitar.wav) and note the
       elapsed render time shown in diagnostics.
    3. Run the official corpus validation from the diagnostics panel.
    4. Confirm all 11 official programs report pass and no NaN/Infinity errors.
  </how-to-verify>
  <resume-signal>Type "approved" if performance is <2s and corpus passes, or report failures.</resume-signal>
</task>

</tasks>

<verification>
- `npm run typecheck`
- Diagnostics panel shows render time and corpus run results
</verification>

<success_criteria>
- Render timing is captured and visible for every simulation run.
- Official corpus results are visible and can be verified by the user.
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-simulation-engine/02-09-SUMMARY.md`
</output>
