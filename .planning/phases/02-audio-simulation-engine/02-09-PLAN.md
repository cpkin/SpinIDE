---
phase: 02-audio-simulation-engine
plan: 09
type: execute
wave: 9
depends_on: [02-08]
files_modified:
  - src/audio/renderSimulation.ts
  - src/audio/renderTypes.ts
  - src/store/audioStore.ts
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Render diagnostics report elapsed time for each render"
    - "Render pipeline applies performance optimizations and flags renders over 2s"
  artifacts:
    - path: "src/audio/renderSimulation.ts"
      provides: "Render timing capture and performance fast path"
    - path: "src/audio/renderTypes.ts"
      provides: "Render timing metadata and performance warnings"
  key_links:
    - from: "src/audio/renderSimulation.ts"
      to: "src/store/audioStore.ts"
      via: "render result state update"
      pattern: "render"
---

<objective>
Instrument render performance and apply render optimizations.

Purpose: Close remaining Phase 2 gaps tied to render performance targets.
Output: Render timing metrics and optimized render loop.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-audio-simulation-engine/02-VERIFICATION.md
@.planning/phases/02-audio-simulation-engine/02-06-SUMMARY.md
@src/audio/renderSimulation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture render timing metrics and persist in state</name>
  <files>src/audio/renderSimulation.ts, src/audio/renderTypes.ts, src/store/audioStore.ts</files>
  <action>
    Measure render duration using performance.now around the renderSimulation execution
    and include elapsed milliseconds in the render result type. Persist the timing
    metadata in audioStore alongside the latest render output so the UI can display it.
    Keep existing render warnings/progress handling intact.
  </action>
  <verify>npm run typecheck</verify>
  <done>Each render result includes an elapsed time value in state.</done>
</task>

<task type="auto">
  <name>Task 2: Optimize render loop and warn on slow renders</name>
  <files>src/audio/renderSimulation.ts, src/audio/renderTypes.ts</files>
  <action>
    Add a render fast path that precomputes handler functions/operands for the compiled program
    (avoid per-instruction getHandler lookups inside the per-sample loop) and hoist reusable values
    to reduce per-sample overhead without changing output. After timing is captured, add a
    RenderWarning when a 30-second render exceeds 2000ms so the UI can surface performance risk.
    Keep existing progress, warning, and normalization behavior intact.
  </action>
  <verify>npm run typecheck</verify>
  <done>Render loop uses cached handlers and emits a warning when render time exceeds 2s.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
</verification>

<success_criteria>
- Render timing is captured and visible for every simulation run.
- Render optimization is in place with warnings when the 2s target is exceeded.
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-simulation-engine/02-09-SUMMARY.md`
</output>
