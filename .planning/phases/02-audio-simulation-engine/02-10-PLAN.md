---
phase: 02-audio-simulation-engine
plan: 10
type: execute
wave: 10
depends_on: [02-09]
files_modified:
  - src/ui/SimulationDiagnostics.tsx
  - src/ui/SimulationPanel.tsx
autonomous: false
gap_closure: true
must_haves:
  truths:
    - "Diagnostics panel shows render timing and performance warnings"
    - "Official corpus results are visible in the diagnostics panel"
    - "Human verification can confirm performance and corpus status"
  artifacts:
    - path: "src/ui/SimulationDiagnostics.tsx"
      provides: "Corpus + performance status display"
    - path: "src/ui/SimulationPanel.tsx"
      provides: "Corpus run trigger entry point"
  key_links:
    - from: "src/ui/SimulationDiagnostics.tsx"
      to: "src/store/audioStore.ts"
      via: "diagnostics display"
      pattern: "corpus|render"
    - from: "src/ui/SimulationDiagnostics.tsx"
      to: "src/fv1/validation/corpusRunner.ts"
      via: "corpus run trigger"
      pattern: "runOfficialCorpus"
---

<objective>
Show render timing and corpus validation status in the diagnostics UI, then run human verification checks.

Purpose: Close remaining Phase 2 gaps tied to corpus verification visibility and performance confirmation.
Output: Diagnostics UI updates and a human verification checkpoint.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-audio-simulation-engine/02-VERIFICATION.md
@.planning/phases/02-audio-simulation-engine/02-06-SUMMARY.md
@src/ui/SimulationDiagnostics.tsx
@src/ui/SimulationPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Show render timing and corpus status in diagnostics UI</name>
  <files>src/ui/SimulationDiagnostics.tsx, src/ui/SimulationPanel.tsx</files>
  <action>
    Display the latest render timing (ms) and any performance warning in SimulationDiagnostics
    using the values stored in audioStore. Ensure corpus results are visible with a clear
    last-run indicator, and wire a UI control so users can trigger the official corpus run
    on demand (reuse runOfficialCorpus and current status handling). Keep layout consistent
    with existing diagnostics styling.
  </action>
  <verify>npm run typecheck</verify>
  <done>Diagnostics panel shows render timing, performance warnings, and corpus results after a run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Diagnostics UI showing render timing, performance warnings, and corpus status.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the Simulation panel.
    2. Render a 30-second demo (e.g., basic-delay.spn with guitar.wav) and note the
       elapsed render time and any performance warning shown in diagnostics.
    3. Trigger the official corpus validation from the diagnostics UI.
    4. Confirm all 11 official programs report pass and no NaN/Infinity errors.
  </how-to-verify>
  <resume-signal>Type "approved" if performance is <2s and corpus passes, or report failures.</resume-signal>
</task>

</tasks>

<verification>
- `npm run typecheck`
- Diagnostics panel shows render time and corpus run results
</verification>

<success_criteria>
- Render timing and performance warnings are visible after each simulation run.
- Official corpus results are visible and can be triggered from the diagnostics UI.
- Human verification confirms performance and corpus status.
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-simulation-engine/02-10-SUMMARY.md`
</output>
