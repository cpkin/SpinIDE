---
phase: 02-audio-simulation-engine
plan: 07
type: execute
wave: 7
depends_on: [02-06]
files_modified:
  - src/fv1/types.ts
  - src/fv1/state.ts
  - src/fv1/constants.ts
  - src/fv1/interpreter.ts
  - src/fv1/instructions/io.ts
  - src/fv1/instructions/delay.ts
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "LFO-driven instructions (WLDS/WLDR/CHO) modulate signals over time"
    - "LFO phase state advances per sample and can be reset via JAM"
  artifacts:
    - path: "src/fv1/types.ts"
      provides: "LFO state fields on FV1State"
    - path: "src/fv1/state.ts"
      provides: "LFO state initialization and reset"
    - path: "src/fv1/instructions/io.ts"
      provides: "WLDS/WLDR/JAM/CHO opcode handlers"
    - path: "src/fv1/interpreter.ts"
      provides: "Per-sample LFO phase updates"
  key_links:
    - from: "src/fv1/interpreter.ts"
      to: "src/fv1/state.ts"
      via: "LFO phase update per sample"
      pattern: "lfo"
    - from: "src/fv1/instructions/io.ts"
      to: "src/fv1/state.ts"
      via: "LFO rate + phase usage"
      pattern: "lfo"
---

<objective>
Implement FV-1 LFO state tracking and LFO-driven instruction handlers so modulation programs execute correctly.

Purpose: Close the critical opcode gap preventing chorus/tremolo/flanger effects from working.
Output: LFO state fields, per-sample LFO updates, and WLDS/WLDR/JAM/CHO handler implementations.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-audio-simulation-engine/02-VERIFICATION.md
@.planning/phases/02-audio-simulation-engine/02-02-SUMMARY.md
@src/fv1/instructions/io.ts
@src/fv1/interpreter.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LFO state fields and initialization</name>
  <files>src/fv1/types.ts, src/fv1/state.ts, src/fv1/constants.ts</files>
  <action>
    Extend FV1State with LFO phase/rate state for both sine and ramp oscillators
    (sin0/sin1, rmp0/rmp1). Initialize/reset these values in createState/resetState,
    and add any constants needed for LFO phase increment scaling at 32 kHz. Keep
    fixed-point ranges consistent with existing fixedPoint helpers and avoid changing
    existing ACC/PACC initialization behavior.
  </action>
  <verify>npm run typecheck</verify>
  <done>LFO phase/rate fields exist on state and are initialized/reset deterministically.</done>
</task>

<task type="auto">
  <name>Task 2: Implement WLDS/WLDR/JAM/CHO handlers with per-sample LFO updates</name>
  <files>src/fv1/interpreter.ts, src/fv1/instructions/io.ts, src/fv1/instructions/delay.ts</files>
  <action>
    Implement WLDS and WLDR to set LFO frequency/phase parameters on state and JAM to
    reset ramp LFO phase. Add per-sample LFO phase advancement in the interpreter loop
    before executing instructions. Implement CHO variants (RDA, SOF, RDAL) to apply
    sine or ramp LFO modulation to delay reads/scaling, and update RMPA handling to use
    LFO modulation where documented. Follow existing fixed-point saturation and delay
    memory conventions; avoid altering non-LFO opcode behavior.
  </action>
  <verify>npm run typecheck</verify>
  <done>LFO-based instructions produce time-varying modulation without TODO stubs remaining.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
- Spot-check an LFO demo (e.g., auto-pan.spn) produces non-static modulation when rendered
</verification>

<success_criteria>
- LFO state advances per sample and LFO opcodes affect output as intended.
- WLDS/WLDR/JAM/CHO TODO stubs are fully implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-simulation-engine/02-07-SUMMARY.md`
</output>
