---
phase: 02-audio-simulation-engine
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - src/parser/opcodes.ts
  - src/fv1/types.ts
  - src/fv1/compileProgram.ts
  - src/fv1/instructions/index.ts
  - src/fv1/instructions/arithmetic.ts
  - src/fv1/instructions/control.ts
  - src/fv1/instructions/delay.ts
  - src/fv1/instructions/io.ts
  - src/fv1/warnings.ts
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Parser recognizes the full SPINAsm instruction list and instructionHandlers cover each opcode"
    - "RAW instructions execute decoded ops or emit explicit unsupported warnings"
  artifacts:
    - path: "src/parser/opcodes.ts"
      provides: "Full SPINAsm opcode catalog (40+ mnemonics)"
    - path: "src/fv1/compileProgram.ts"
      provides: "Operand parsing for every opcode in the catalog"
    - path: "src/fv1/instructions/index.ts"
      provides: "Instruction handler registry for all opcodes"
    - path: "src/fv1/instructions/io.ts"
      provides: "RAW opcode handler with decode logic"
  key_links:
    - from: "src/fv1/compileProgram.ts"
      to: "src/parser/opcodes.ts"
      via: "opcode list"
      pattern: "opcodes"
    - from: "src/fv1/instructions/index.ts"
      to: "src/fv1/instructions/io.ts"
      via: "raw handler mapping"
      pattern: "raw"
    - from: "src/fv1/instructions/io.ts"
      to: "src/fv1/instructions/index.ts"
      via: "decoded opcode dispatch"
      pattern: "getHandler|instructionHandlers"
---

<objective>
Close the remaining opcode coverage gap by expanding the opcode catalog and implementing RAW decoding.

Purpose: Ensure the interpreter truly covers the full SPINAsm instruction set and no RAW opcodes are stubbed.
Output: Expanded opcode list, handler registry coverage, and RAW execution support.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-audio-simulation-engine/02-VERIFICATION.md
@docs/spinasm-spec.md
@src/parser/opcodes.ts
@src/fv1/compileProgram.ts
@src/fv1/instructions/index.ts
@src/fv1/instructions/io.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand opcode catalog and handler coverage to 40+ mnemonics</name>
  <files>src/parser/opcodes.ts, src/fv1/types.ts, src/fv1/compileProgram.ts, src/fv1/instructions/index.ts, src/fv1/instructions/arithmetic.ts, src/fv1/instructions/control.ts, src/fv1/instructions/delay.ts, src/fv1/instructions/io.ts</files>
  <action>
    Audit the SPINAsm instruction list against the official manual/asfv1 table referenced in
    `docs/spinasm-spec.md`, then update `src/parser/opcodes.ts` to include every mnemonic so the
    list reaches 40+ entries. Extend `compileProgram` operand parsing and `src/fv1/types.ts`
    definitions to cover each newly added opcode. Implement handlers for any missing opcodes in
    the appropriate instruction modules and register them in `src/fv1/instructions/index.ts` so
    no opcode in the list maps to NOP unless that is explicitly documented as unsupported.
  </action>
  <verify>npm run typecheck</verify>
  <done>Opcode catalog contains 40+ mnemonics and every opcode has a concrete handler.</done>
</task>

<task type="auto">
  <name>Task 2: Implement RAW instruction decoding and execution path</name>
  <files>src/fv1/instructions/io.ts, src/fv1/compileProgram.ts, src/fv1/warnings.ts</files>
  <action>
    Update RAW parsing in `compileProgram` to treat the operand as an unsigned 32-bit integer and
    pass the raw word through unchanged. Implement RAW execution in `src/fv1/instructions/io.ts`
    by decoding the raw word into opcode + operands using the SPINAsm encoding tables (bit fields
    for opcode, register, coefficient, and flags). Dispatch the decoded opcode through
    `getHandler` and execute it with reconstructed operands. If the raw opcode cannot be decoded
    or maps to an unsupported opcode, emit a warning via `src/fv1/warnings.ts` and execute NOP.
  </action>
  <verify>npm run typecheck</verify>
  <done>RAW instructions execute a decoded opcode or emit a clear unsupported warning.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
- `src/parser/opcodes.ts` contains 40+ opcodes and `instructionHandlers` maps each
- RAW handler decodes and dispatches or warns for unsupported words
</verification>

<success_criteria>
- Instruction registry covers the full SPINAsm opcode list with no TODO stubs.
- RAW instructions no longer silently no-op; they execute decoded ops or warn explicitly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-simulation-engine/02-11-SUMMARY.md`
</output>
