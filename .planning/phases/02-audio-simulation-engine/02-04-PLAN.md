---
phase: 02-audio-simulation-engine
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - src/store/audioStore.ts
  - src/ui/SimulationPanel.tsx
  - src/ui/ProgressBar.tsx
  - src/demos/index.ts
  - src/App.tsx
  - src/styles/app.css
  - public/demos/guitar.wav
  - public/demos/synth.wav
  - public/demos/drums.wav
  - public/demos/voice.wav
autonomous: true
must_haves:
  truths:
    - "User can load demo or uploaded audio and see it queued for simulation"
    - "Audio is resampled to 32 kHz before simulation and user is informed"
    - "Simulation runs using FV-1 fixed-point math with 32-sample pot timing"
    - "All IO modes (mono_mono, mono_stereo, stereo_stereo) produce output audio"
    - "Long renders show progress and respect 30s default / 2min max limits"
    - "Simulation limitations are visible in the UI"
    - "Official Spin demo programs can be run through the simulator without errors"
  artifacts:
    - path: "src/fv1/fixedPoint.ts"
      provides: "1.23 fixed-point helpers and saturation"
    - path: "src/fv1/interpreter.ts"
      provides: "FV-1 sample execution loop"
    - path: "src/audio/renderSimulation.ts"
      provides: "Offline render pipeline with limits/progress"
    - path: "src/ui/SimulationPanel.tsx"
      provides: "Audio upload/demo selection and render controls"
    - path: "src/fv1/validation/corpusRunner.ts"
      provides: "Official corpus simulation checks"
  key_links:
    - from: "src/ui/SimulationPanel.tsx"
      to: "src/audio/renderSimulation.ts"
      via: "render button handler"
      pattern: "renderSimulation\\("
    - from: "src/audio/renderSimulation.ts"
      to: "src/fv1/interpreter.ts"
      via: "per-sample execution"
      pattern: "runProgram|executeProgram"
    - from: "src/fv1/validation/corpusRunner.ts"
      to: "tests/corpus/official"
      via: "import.meta.glob"
      pattern: "official/.*\\.spn"
---

<objective>
Expose simulation UI controls, demos, and wire editor source into the render flow.

Purpose: Let users select audio, compile the current editor source, and trigger renders with progress.
Output: Simulation panel UI, demo assets, and editor-to-render wiring.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-audio-simulation-engine/02-CONTEXT.md
@.planning/phases/02-audio-simulation-engine/02-RESEARCH.md
@src/App.tsx
@src/store/validationStore.ts
@src/parser/parseSpinAsm.ts
@src/fv1/compileProgram.ts
@src/styles/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build simulation panel UI and input validation</name>
  <files>src/store/audioStore.ts, src/ui/SimulationPanel.tsx, src/ui/ProgressBar.tsx</files>
  <action>
    Create an audio store to track selected demo/upload, IO mode, normalization toggles,
    render status, output buffer, and render errors/warnings. Add a SimulationPanel with
    upload/drag-drop, demo selection, IO mode control, render button, progress bar,
    and playback control. Explicitly support WAV/MP3/M4A by setting the file input accept
    list and validating file type/extension before decode, surfacing a friendly error
    message when unsupported files or decode failures occur.
  </action>
  <verify>npm run dev</verify>
  <done>Users can select audio, see validation errors for bad files, and track render progress.</done>
</task>

<task type="auto">
  <name>Task 2: Add demo assets and layout styling</name>
  <files>src/demos/index.ts, src/App.tsx, src/styles/app.css, public/demos/guitar.wav, public/demos/synth.wav, public/demos/drums.wav, public/demos/voice.wav</files>
  <action>
    Add four demo audio files under public/demos and index metadata in src/demos/index.ts.
    Update App layout/styles to include the simulation panel alongside existing validation UI.
  </action>
  <verify>npm run dev</verify>
  <done>Demo assets appear in the UI and the layout accommodates the simulation panel.</done>
</task>

<task type="auto">
  <name>Task 3: Wire editor source into simulation render flow</name>
  <files>src/ui/SimulationPanel.tsx, src/store/audioStore.ts</files>
  <action>
    On render, read the current editor source from validationStore, parse it with
    parseSpinAsm, and compile instructions via compileProgram. Block render and surface
    a store error when parse/compile fails, otherwise pass the compiled instruction array,
    IO mode selection, and pot values into renderSimulation. Keep the render action
    tied to the current editor source so updates recompile before rendering.
  </action>
  <verify>npm run dev</verify>
  <done>Render uses the current editor source and shows a clear error if parsing or compilation fails.</done>
</task>

</tasks>

<verification>
- `npm run dev` and confirm demo upload + render completes using current editor source
</verification>

<success_criteria>
- Simulation UI provides demo selection, upload, IO mode controls, and progress/limit messaging.
- Editor source is parsed and compiled before rendering, with clear errors on failure.
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-simulation-engine/02-04-SUMMARY.md`
</output>
