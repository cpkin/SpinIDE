---
phase: 02-audio-simulation-engine
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - src/audio/playback.ts
  - src/store/audioStore.ts
  - src/ui/SimulationPanel.tsx
  - src/ui/ProgressBar.tsx
autonomous: true
must_haves:
  truths:
    - "User can upload audio and see validation errors"
    - "Render uses current editor source and surfaces compile errors"
    - "User can play rendered audio output"
    - "Simulation panel shows render progress and playback controls"
  artifacts:
    - path: "src/audio/playback.ts"
      provides: "Web Audio playback helper for rendered buffers"
    - path: "src/store/audioStore.ts"
      provides: "Simulation UI state and render errors"
    - path: "src/ui/SimulationPanel.tsx"
      provides: "Simulation controls and render trigger"
    - path: "src/ui/ProgressBar.tsx"
      provides: "Render progress display"
  key_links:
    - from: "src/ui/SimulationPanel.tsx"
      to: "src/audio/renderSimulation.ts"
      via: "render button handler"
      pattern: "renderSimulation\\("
    - from: "src/ui/SimulationPanel.tsx"
      to: "src/parser/parseSpinAsm.ts"
      via: "editor source parse"
      pattern: "parseSpinAsm"
    - from: "src/ui/SimulationPanel.tsx"
      to: "src/audio/playback.ts"
      via: "render output playback"
      pattern: "playback"
---

<objective>
Expose simulation UI controls and wire editor source into the render flow.

Purpose: Let users upload audio, compile the current editor source, and trigger renders with progress.
Output: Simulation panel UI and editor-to-render wiring.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-audio-simulation-engine/02-CONTEXT.md
@.planning/phases/02-audio-simulation-engine/02-RESEARCH.md
@src/App.tsx
@src/store/validationStore.ts
@src/parser/parseSpinAsm.ts
@src/fv1/compileProgram.ts
@src/styles/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build simulation panel UI and input validation</name>
  <files>src/store/audioStore.ts, src/ui/SimulationPanel.tsx, src/ui/ProgressBar.tsx</files>
  <action>
    Create an audio store to track selected demo/upload, IO mode, normalization toggles,
    render status, output buffer, and render errors/warnings. Add a SimulationPanel with
    upload/drag-drop, IO mode control, render button, progress bar,
    and playback control. Explicitly support WAV/MP3/M4A by setting the file input accept
    list and validating file type/extension before decode, surfacing a friendly error
    message when unsupported files or decode failures occur.
  </action>
  <verify>npm run dev</verify>
  <done>Users can upload audio, see validation errors for bad files, and track render progress.</done>
</task>

<task type="auto">
  <name>Task 2: Wire render output to playback controls</name>
  <files>src/audio/playback.ts, src/ui/SimulationPanel.tsx, src/store/audioStore.ts</files>
  <action>
    Create a small playback helper around AudioContext and AudioBufferSourceNode that can
    play, stop, and report playback state for a rendered AudioBuffer. Store the latest
    render output in audioStore, expose play/stop actions, and wire the SimulationPanel
    playback controls so they operate on the most recent render output. Disable playback
    controls when no render output is available and surface a friendly message if playback
    fails to start.
  </action>
  <verify>npm run dev</verify>
  <done>Render output can be played back with audible audio via the Simulation panel controls.</done>
</task>

<task type="auto">
  <name>Task 3: Wire editor source into simulation render flow</name>
  <files>src/ui/SimulationPanel.tsx, src/store/audioStore.ts</files>
  <action>
    On render, read the current editor source from validationStore, parse it with
    parseSpinAsm, and compile instructions via compileProgram. Block render and surface
    a store error when parse/compile fails, otherwise pass the compiled instruction array,
    IO mode selection, and pot values into renderSimulation. Keep the render action
    tied to the current editor source so updates recompile before rendering.
  </action>
  <verify>npm run dev</verify>
  <done>Render uses the current editor source and shows a clear error if parsing or compilation fails.</done>
</task>

</tasks>

<verification>
- `npm run dev` and confirm upload + render completes using current editor source
- Render output is playable via Simulation panel controls
</verification>

<success_criteria>
- Simulation UI provides upload, IO mode controls, and progress/limit messaging.
- Rendered audio can be played back through the Simulation panel controls.
- Editor source is parsed and compiled before rendering, with clear errors on failure.
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-simulation-engine/02-04-SUMMARY.md`
</output>
