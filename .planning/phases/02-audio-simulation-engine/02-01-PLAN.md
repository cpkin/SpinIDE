---
phase: 02-audio-simulation-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fv1/constants.ts
  - src/fv1/fixedPoint.ts
  - src/fv1/types.ts
  - src/fv1/state.ts
  - src/fv1/interpreter.ts
  - src/fv1/instructions/index.ts
autonomous: true
must_haves:
  truths:
    - "User can load demo or uploaded audio and see it queued for simulation"
    - "Audio is resampled to 32 kHz before simulation and user is informed"
    - "Simulation runs using FV-1 fixed-point math with 32-sample pot timing"
    - "All IO modes (mono_mono, mono_stereo, stereo_stereo) produce output audio"
    - "Long renders show progress and respect 30s default / 2min max limits"
    - "Simulation limitations are visible in the UI"
    - "Official Spin demo programs can be run through the simulator without errors"
  artifacts:
    - path: "src/fv1/fixedPoint.ts"
      provides: "1.23 fixed-point helpers and saturation"
    - path: "src/fv1/interpreter.ts"
      provides: "FV-1 sample execution loop"
    - path: "src/audio/renderSimulation.ts"
      provides: "Offline render pipeline with limits/progress"
    - path: "src/ui/SimulationPanel.tsx"
      provides: "Audio upload/demo selection and render controls"
    - path: "src/fv1/validation/corpusRunner.ts"
      provides: "Official corpus simulation checks"
  key_links:
    - from: "src/ui/SimulationPanel.tsx"
      to: "src/audio/renderSimulation.ts"
      via: "render button handler"
      pattern: "renderSimulation\\("
    - from: "src/audio/renderSimulation.ts"
      to: "src/fv1/interpreter.ts"
      via: "per-sample execution"
      pattern: "runProgram|executeProgram"
    - from: "src/fv1/validation/corpusRunner.ts"
      to: "tests/corpus/official"
      via: "import.meta.glob"
      pattern: "official/.*\\.spn"
---

<objective>
Establish the FV-1 fixed-point math and interpreter foundation needed for audio simulation.

Purpose: Provide accurate 1.23 arithmetic and state scaffolding before opcode implementation.
Output: Fixed-point helpers, state model, and interpreter loop skeleton.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-audio-simulation-engine/02-CONTEXT.md
@.planning/phases/02-audio-simulation-engine/02-RESEARCH.md
@src/parser/ast.ts
@src/parser/opcodes.ts
@src/parser/parseSpinAsm.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FV-1 fixed-point helpers</name>
  <files>src/fv1/constants.ts, src/fv1/fixedPoint.ts</files>
  <action>
    Define 1.23 format constants (fraction bits, max/min values, coefficient ranges) and implement helpers for:
    - floatToFixed and fixedToFloat conversions
    - saturating add/sub/mul with 24-bit two's-complement clamp
    - shift helpers for LOG/EXP and instruction-specific scaling
    - coefficient clamping for RDA/RDAX ranges

    Ensure helpers return numeric types suitable for fast per-sample math.
  </action>
  <verify>npm run typecheck</verify>
  <done>Fixed-point helpers expose conversion + saturation functions for 1.23 math.</done>
</task>

<task type="auto">
  <name>Task 2: Model FV-1 interpreter state</name>
  <files>src/fv1/types.ts, src/fv1/state.ts</files>
  <action>
    Create types for FV-1 state and program instructions. Include ACC, PACC, LR, DAC outputs, 16 registers,
    delay RAM (Float32Array length 32768), pot values, and IO mode. Provide createState/resetState helpers
    with deterministic defaults and explicit pot value ranges.
  </action>
  <verify>npm run typecheck</verify>
  <done>Interpreter state can be created and reset with all core registers/memory initialized.</done>
</task>

<task type="auto">
  <name>Task 3: Build interpreter loop skeleton</name>
  <files>src/fv1/interpreter.ts, src/fv1/instructions/index.ts</files>
  <action>
    Implement the sample-by-sample execution loop that:
    - steps 128 instructions per sample
    - updates PACC from previous ACC each sample
    - calls a 32-sample block hook for pot updates
    - dispatches opcodes via a handler registry with a NOP default
    Expose a runProgram/executeProgram API that accepts compiled instructions and input buffers,
    returning output buffers without UI concerns.
  </action>
  <verify>npm run typecheck</verify>
  <done>Interpreter skeleton executes 128-instruction loops with 32-sample block hook and opcode dispatch.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
</verification>

<success_criteria>
- Fixed-point helpers cover conversion, saturation, and coefficient clamping for 1.23 format.
- Interpreter state and loop compile, with 128-step execution and 32-sample block hook in place.
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-simulation-engine/02-01-SUMMARY.md`
</output>
