---
phase: 02-audio-simulation-engine
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/fv1/instructions/arithmetic.ts
  - src/fv1/instructions/control.ts
  - src/fv1/instructions/delay.ts
  - src/fv1/instructions/io.ts
  - src/fv1/instructions/index.ts
  - src/fv1/compileProgram.ts
  - src/fv1/io.ts
  - src/fv1/interpreter.ts
  - src/fv1/types.ts
autonomous: true
must_haves:
  truths:
    - "User can load demo or uploaded audio and see it queued for simulation"
    - "Audio is resampled to 32 kHz before simulation and user is informed"
    - "Simulation runs using FV-1 fixed-point math with 32-sample pot timing"
    - "All IO modes (mono_mono, mono_stereo, stereo_stereo) produce output audio"
    - "Long renders show progress and respect 30s default / 2min max limits"
    - "Simulation limitations are visible in the UI"
    - "Official Spin demo programs can be run through the simulator without errors"
  artifacts:
    - path: "src/fv1/fixedPoint.ts"
      provides: "1.23 fixed-point helpers and saturation"
    - path: "src/fv1/interpreter.ts"
      provides: "FV-1 sample execution loop"
    - path: "src/audio/renderSimulation.ts"
      provides: "Offline render pipeline with limits/progress"
    - path: "src/ui/SimulationPanel.tsx"
      provides: "Audio upload/demo selection and render controls"
    - path: "src/fv1/validation/corpusRunner.ts"
      provides: "Official corpus simulation checks"
  key_links:
    - from: "src/ui/SimulationPanel.tsx"
      to: "src/audio/renderSimulation.ts"
      via: "render button handler"
      pattern: "renderSimulation\\("
    - from: "src/audio/renderSimulation.ts"
      to: "src/fv1/interpreter.ts"
      via: "per-sample execution"
      pattern: "runProgram|executeProgram"
    - from: "src/fv1/validation/corpusRunner.ts"
      to: "tests/corpus/official"
      via: "import.meta.glob"
      pattern: "official/.*\\.spn"
---

<objective>
Implement the FV-1 opcode handlers and compile SpinASM AST into executable instructions with IO mode support.

Purpose: Make the interpreter execute real SpinASM programs and honor FV-1 IO semantics.
Output: Complete opcode handler set, AST compiler, and IO mode mapping in the interpreter.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-audio-simulation-engine/02-CONTEXT.md
@.planning/phases/02-audio-simulation-engine/02-RESEARCH.md
@src/parser/ast.ts
@src/parser/opcodes.ts
@src/parser/parseSpinAsm.ts
@src/analysis/analysisPipeline.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement FV-1 opcode handlers</name>
  <files>src/fv1/instructions/arithmetic.ts, src/fv1/instructions/control.ts, src/fv1/instructions/delay.ts, src/fv1/instructions/io.ts, src/fv1/instructions/index.ts</files>
  <action>
    Implement handlers for every opcode defined in `src/parser/opcodes.ts`, grouped by arithmetic,
    control flow, delay memory, and IO/LFO behaviors. Use fixed-point helpers for ACC/PACC/LR math,
    apply coefficient range clamping, and honor LOG/EXP scaling conventions. Ensure SKP and CLR-type
    instructions correctly mutate the program counter or ACC state.
  </action>
  <verify>npm run typecheck</verify>
  <done>All SpinASM opcodes in the parser list map to concrete handler implementations.</done>
</task>

<task type="auto">
  <name>Task 2: Compile SpinASM AST into instruction slots</name>
  <files>src/fv1/compileProgram.ts, src/fv1/types.ts</files>
  <action>
    Build a compiler that converts parsed SpinASM instructions into a 128-slot instruction array.
    Resolve labels/targets for SKP, normalize operands (coefficients, registers, delay addresses),
    and fill unused slots with NOPs so the interpreter loop always executes 128 instructions.
  </action>
  <verify>npm run typecheck</verify>
  <done>AST compilation produces a fixed-size instruction array with resolved operands.</done>
</task>

<task type="auto">
  <name>Task 3: Add IO mode mapping and pot timing</name>
  <files>src/fv1/io.ts, src/fv1/interpreter.ts</files>
  <action>
    Implement input/output channel mapping for mono_mono, mono_stereo, and stereo_stereo modes.
    Update pot values only every 32 samples (block timing), and expose an interpreter option
    for input normalization (default off) plus output normalization to -1 dB.
  </action>
  <verify>npm run typecheck</verify>
  <done>Interpreter accepts IO modes and updates pots on 32-sample boundaries.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
</verification>

<success_criteria>
- All opcodes listed in `src/parser/opcodes.ts` execute with fixed-point semantics.
- Parsed programs compile into 128-slot instruction arrays and run with correct IO modes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-simulation-engine/02-02-SUMMARY.md`
</output>
