---
phase: 02-audio-simulation-engine
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/parser/opcodes.ts
  - src/fv1/instructions/arithmetic.ts
  - src/fv1/instructions/control.ts
  - src/fv1/instructions/delay.ts
  - src/fv1/instructions/io.ts
  - src/fv1/instructions/index.ts
  - src/fv1/compileProgram.ts
  - src/fv1/io.ts
  - src/fv1/interpreter.ts
  - src/fv1/types.ts
autonomous: true
must_haves:
  truths:
    - "SpinASM programs compile and execute with the full 40+ FV-1 opcode set"
    - "Labelled jumps compile without unresolved target failures"
    - "IO modes route channels correctly during execution"
  artifacts:
    - path: "src/parser/opcodes.ts"
      provides: "Full FV-1 opcode list for parser recognition"
    - path: "src/fv1/instructions/arithmetic.ts"
      provides: "Arithmetic opcode handlers"
    - path: "src/fv1/instructions/control.ts"
      provides: "Control flow opcode handlers"
    - path: "src/fv1/instructions/delay.ts"
      provides: "Delay/RAM opcode handlers"
    - path: "src/fv1/instructions/io.ts"
      provides: "IO/LFO opcode handlers"
    - path: "src/fv1/compileProgram.ts"
      provides: "SpinASM AST to instruction compilation"
    - path: "src/fv1/io.ts"
      provides: "IO mode mapping"
    - path: "src/fv1/interpreter.ts"
      provides: "IO mode and pot timing integration"
    - path: "src/fv1/types.ts"
      provides: "Instruction operand/type definitions"
  key_links:
    - from: "src/fv1/compileProgram.ts"
      to: "src/parser/opcodes.ts"
      via: "opcode mapping"
      pattern: "opcodes"
    - from: "src/fv1/interpreter.ts"
      to: "src/fv1/instructions/index.ts"
      via: "handler dispatch"
      pattern: "handlers"
    - from: "src/fv1/interpreter.ts"
      to: "src/fv1/io.ts"
      via: "IO mode mapping"
      pattern: "map|ioMode"
---

<objective>
Implement the FV-1 opcode handlers and compile SpinASM AST into executable instructions with IO mode support.

Purpose: Make the interpreter execute real SpinASM programs and honor FV-1 IO semantics.
Output: Complete opcode handler set, AST compiler, and IO mode mapping in the interpreter.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-audio-simulation-engine/02-CONTEXT.md
@.planning/phases/02-audio-simulation-engine/02-RESEARCH.md
@src/parser/ast.ts
@src/parser/opcodes.ts
@src/parser/parseSpinAsm.ts
@src/analysis/analysisPipeline.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit opcode coverage and expand parser/compiler mappings</name>
  <files>src/parser/opcodes.ts, src/fv1/compileProgram.ts, src/fv1/types.ts</files>
  <action>
    Audit the FV-1 instruction list against official docs and expand `src/parser/opcodes.ts` to
    include the full 40+ opcode set (beyond the current 29). Update compileProgram operand parsing
    to normalize operands for newly added opcodes (register, delay, coefficient, and control-flow
    forms), and extend instruction operand/type definitions in `src/fv1/types.ts` as needed so the
    compiler emits valid instruction operands for every opcode in the updated list.
  </action>
  <verify>npm run typecheck</verify>
  <done>Parser opcode list is expanded to the full FV-1 set and compiler handles operands for each.</done>
</task>

  <task type="auto">
    <name>Task 2: Implement FV-1 opcode handlers for the expanded list</name>
    <files>src/fv1/instructions/arithmetic.ts, src/fv1/instructions/control.ts, src/fv1/instructions/delay.ts, src/fv1/instructions/io.ts, src/fv1/instructions/index.ts</files>
    <action>
      Implement handlers for every opcode defined in the updated `src/parser/opcodes.ts`, grouped by
      arithmetic, control flow, delay memory, and IO/LFO behaviors. Use fixed-point helpers for
      ACC/PACC/LR math, apply coefficient range clamping, honor LOG/EXP scaling conventions, and
      ensure SKP/CLR-type instructions correctly mutate the program counter or ACC state. Ensure
      the instruction registry includes all opcodes with no TODO stubs remaining.
    </action>
  <verify>npm run typecheck</verify>
  <done>All FV-1 opcodes in the parser list map to concrete handler implementations.</done>
</task>

<task type="auto">
  <name>Task 3: Add IO mode mapping and pot timing</name>
  <files>src/fv1/io.ts, src/fv1/interpreter.ts</files>
  <action>
    Implement input/output channel mapping for mono_mono, mono_stereo, and stereo_stereo modes.
    Update pot values only every 32 samples (block timing), and expose an interpreter option
    for input normalization (default off) plus output normalization to -1 dB.
  </action>
  <verify>npm run typecheck</verify>
  <done>Interpreter accepts IO modes and updates pots on 32-sample boundaries.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
</verification>

<success_criteria>
- Parser recognizes the full 40+ FV-1 opcode list and every opcode executes with fixed-point semantics.
- Parsed programs compile into 128-slot instruction arrays and run with correct IO modes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-simulation-engine/02-02-SUMMARY.md`
</output>
