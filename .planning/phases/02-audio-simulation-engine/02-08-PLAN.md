---
phase: 02-audio-simulation-engine
plan: 08
type: execute
wave: 8
depends_on: [02-07]
files_modified:
  - src/fv1/types.ts
  - src/fv1/state.ts
  - src/fv1/interpreter.ts
  - src/fv1/instructions/control.ts
  - src/fv1/instructions/arithmetic.ts
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "SKP/JMP alter program flow based on FV-1 conditions"
    - "LDAX ADCL/ADCR reads current input samples"
  artifacts:
    - path: "src/fv1/instructions/control.ts"
      provides: "SKP/JMP condition evaluation and targets"
    - path: "src/fv1/interpreter.ts"
      provides: "Program counter updates for skips and jumps"
    - path: "src/fv1/instructions/arithmetic.ts"
      provides: "LDAX handling for ADCL/ADCR"
    - path: "src/fv1/state.ts"
      provides: "ADC storage for current input samples"
  key_links:
    - from: "src/fv1/instructions/control.ts"
      to: "src/fv1/interpreter.ts"
      via: "skip/jump signaling"
      pattern: "skp|jmp"
    - from: "src/fv1/interpreter.ts"
      to: "src/fv1/instructions/arithmetic.ts"
      via: "LDAX reading adcL/adcR"
      pattern: "adcl|adcr"
---

<objective>
Fix control flow and ADC register access so programs with loops, conditionals, and input reads execute correctly.

Purpose: Close remaining instruction gaps blocking corpus validation and real-world programs.
Output: Working SKP/JMP program counter control and LDAX ADCL/ADCR input sampling.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-audio-simulation-engine/02-VERIFICATION.md
@.planning/phases/02-audio-simulation-engine/02-02-SUMMARY.md
@src/fv1/instructions/control.ts
@src/fv1/interpreter.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement SKP/JMP control flow in interpreter</name>
  <files>src/fv1/interpreter.ts, src/fv1/instructions/control.ts, src/fv1/types.ts, src/fv1/state.ts</files>
  <action>
    Add explicit program counter control for SKP and JMP. Extend FV1State with a skip
    counter or nextPc field (choose one and document in code). Update control.ts
    handlers to evaluate skip conditions and provide the skip/jump intent. Update the
    interpreter loop to honor skip/jump requests without breaking the 128-instruction
    limit or POT update cadence. Keep existing instruction dispatch and error handling
    intact for non-control opcodes.
  </action>
  <verify>npm run typecheck</verify>
  <done>SKP/JMP correctly skip or jump to target instructions based on condition flags.</done>
</task>

<task type="auto">
  <name>Task 2: Wire LDAX ADCL/ADCR to input ADC samples</name>
  <files>src/fv1/interpreter.ts, src/fv1/instructions/arithmetic.ts, src/fv1/types.ts, src/fv1/state.ts</files>
  <action>
    Store per-sample ADC values on FV1State before the instruction loop using existing
    mapInputToADC logic. Update LDAX handling to detect ADCL/ADCR pseudo-registers and
    return the stored ADC values instead of reading delay RAM. Preserve existing LDAX
    behavior for register and delay memory accesses.
  </action>
  <verify>npm run typecheck</verify>
  <done>LDAX ADCL/ADCR returns current input samples and no longer reads garbage.</done>
</task>

</tasks>

<verification>
- `npm run typecheck`
- Render a program using SKP/JMP or LDAX ADCL/ADCR without runtime errors
</verification>

<success_criteria>
- Conditional branching and loops execute as expected for SKP/JMP.
- Input-dependent programs read ADC channels correctly via LDAX.
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-simulation-engine/02-08-SUMMARY.md`
</output>
