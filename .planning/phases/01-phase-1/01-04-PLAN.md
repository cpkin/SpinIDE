---
phase: 01-phase-1
plan: 04
type: execute
wave: 4
depends_on:
  - "01-01"
  - "01-03"
files_modified:
  - src/editor/editorExtensions.ts
  - src/editor/SpinEditor.tsx
  - src/store/validationStore.ts
  - src/ui/DiagnosticsPanel.tsx
  - src/ui/ResourceMeters.tsx
  - src/ui/CopyDiagnosticsButton.tsx
  - src/diagnostics/formatCopyPayload.ts
  - src/diagnostics/formatDiagnostics.ts
autonomous: true

must_haves:
  truths:
    - "Diagnostics appear inline (underline + tooltip), in gutter, and in the panel"
    - "Resource meters update live and highlight when limits or warnings occur"
    - "Copy button exports full code plus errors/warnings in a single payload"
  artifacts:
    - path: "src/editor/editorExtensions.ts"
      provides: "CodeMirror lint + update pipeline"
    - path: "src/diagnostics/formatCopyPayload.ts"
      provides: "Clipboard payload formatting"
    - path: "src/ui/ResourceMeters.tsx"
      provides: "Live resource meter rendering"
  key_links:
    - from: "src/editor/editorExtensions.ts"
      to: "src/analysis/analysisPipeline.ts"
      via: "lint source invocation"
      pattern: "analysisPipeline"
    - from: "src/editor/SpinEditor.tsx"
      to: "src/language/spinasmLanguage.ts"
      via: "CodeMirror extensions"
      pattern: "spinasmLanguage|spinasm"
    - from: "src/ui/CopyDiagnosticsButton.tsx"
      to: "src/diagnostics/formatCopyPayload.ts"
      via: "clipboard payload formatter"
      pattern: "formatCopyPayload"
---

<objective>
Wire parsing + analysis into CodeMirror linting, update the UI with live diagnostics and meters, and implement the copy-to-clipboard payload format.

Purpose: Deliver the complete validation UX defined in Phase 1 requirements.
Output: Working editor with inline diagnostics, live meters, and copy button.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-phase-1/01-CONTEXT.md
@.planning/phases/01-phase-1/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Connect analysis pipeline to CodeMirror linting</name>
  <files>src/editor/editorExtensions.ts, src/editor/SpinEditor.tsx, src/store/validationStore.ts</files>
  <action>
    Implement a CodeMirror `linter` source in `editorExtensions.ts` that calls the analysis pipeline on the editor state and returns `Diagnostic[]` with severity mapped to error/warning/info, plus `actions` for suggested fixes. Add `lintGutter()` and lint panel support. Update `SpinEditor` to include the `spinasm` `LanguageSupport` from `src/language/index.ts` in the editor extensions, and trigger parsing on line completion with a debounced update listener, storing diagnostics/resources in the store as they change.
  </action>
  <verify>npm run build</verify>
  <done>Diagnostics appear in the editor gutter, tooltips, and lint panel after edits.</done>
</task>

<task type="auto">
  <name>Task 2: Render live meters and diagnostics list</name>
  <files>src/ui/DiagnosticsPanel.tsx, src/ui/ResourceMeters.tsx</files>
  <action>
    Update `DiagnosticsPanel` to render diagnostics chronologically with severity badges, line/column, and context snippets (±2 lines). Update `ResourceMeters` to display instruction count, delay RAM usage (samples + ms), and register usage count, highlighting meters when limits are exceeded or related warnings exist.
  </action>
  <verify>npm run build</verify>
  <done>UI shows real diagnostics and meters that respond to edits.</done>
</task>

<task type="auto">
  <name>Task 3: Implement copy payload formatting</name>
  <files>src/diagnostics/formatDiagnostics.ts, src/diagnostics/formatCopyPayload.ts, src/ui/CopyDiagnosticsButton.tsx</files>
  <action>
    Create `formatDiagnostics.ts` to normalize diagnostics into human-readable strings (severity, location, message, suggested fix, context). Implement `formatCopyPayload` to include the full source code plus a diagnostics section matching the UX spec from `01-CONTEXT.md`. Update `CopyDiagnosticsButton` to use `navigator.clipboard.writeText` and show a temporary success state.
  </action>
  <verify>npm run build</verify>
  <done>Copy button writes full source + diagnostics payload to clipboard.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds and diagnostics render inline, in gutter, and in panel.
</verification>

<success_criteria>
- Errors and warnings show inline, in gutter, and in diagnostics panel.
- Resource meters update live and highlight when warnings trigger.
- Copy payload includes full source plus formatted diagnostics.
</success_criteria>

<output>
After completion, create `.planning/phases/01-phase-1/01-04-SUMMARY.md`
</output>
