---
phase: 01-phase-1
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/language/spinasm.grammar
  - src/language/spinasmLanguage.ts
  - src/language/index.ts
  - src/parser/ast.ts
  - src/parser/opcodes.ts
  - src/parser/parseSpinAsm.ts
  - src/diagnostics/context.ts
  - src/diagnostics/parseDiagnostics.ts
autonomous: true

must_haves:
  truths:
    - "Syntax errors include line/column positions with ±2 lines of context"
    - "SpinASM grammar provides consistent highlighting for opcodes, labels, and directives"
    - "Valid SpinASM syntax does not raise unexpected parsing errors"
  artifacts:
    - path: "src/language/spinasm.grammar"
      provides: "SpinASM Lezer grammar"
    - path: "src/parser/parseSpinAsm.ts"
      provides: "Parser entry point returning AST + diagnostics"
    - path: "src/diagnostics/context.ts"
      provides: "Line context extraction utilities"
  key_links:
    - from: "src/language/spinasmLanguage.ts"
      to: "src/language/spinasm.grammar"
      via: "Lezer parser import"
      pattern: "parser"
    - from: "src/parser/parseSpinAsm.ts"
      to: "src/diagnostics/context.ts"
      via: "context extraction helper"
      pattern: "getLineContext"
---

<objective>
Define the SpinASM language grammar and build a parser pipeline that produces syntax diagnostics with rich line context for later UI and lint integration.

Purpose: Establish parse accuracy and contextual error reporting before linting and resource analysis.
Output: Lezer-backed SpinASM language support plus parser diagnostics utilities.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-phase-1/01-CONTEXT.md
@.planning/phases/01-phase-1/01-RESEARCH.md
@docs/spinasm-spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Lezer grammar and language support</name>
  <files>src/language/spinasm.grammar, src/language/spinasmLanguage.ts, src/language/index.ts</files>
  <action>
    Create `src/language/spinasm.grammar` based on `docs/spinasm-spec.md` covering labels, instructions, directives, registers, numeric literals, comments, and separators. Add `spinasmLanguage.ts` that imports the generated parser, configures highlight tags via `styleTags`, and exports `spinasmLanguage` plus a `spinasm` `LanguageSupport`. Export from `src/language/index.ts` for easy imports in editor setup.
  </action>
  <verify>npm run typecheck</verify>
  <done>Grammar compiles and exports a `LanguageSupport` for CodeMirror.</done>
</task>

<task type="auto">
  <name>Task 2: Build parser diagnostics with line context</name>
  <files>src/parser/ast.ts, src/parser/opcodes.ts, src/parser/parseSpinAsm.ts, src/diagnostics/context.ts, src/diagnostics/parseDiagnostics.ts</files>
  <action>
    Implement a parser module that reads SpinASM source and returns a structured `ParseResult` containing an instruction list, symbol tables (labels, equates, memory allocations), and diagnostics. Use the Lezer parser to detect syntax errors (`Error` nodes or parse failure ranges) and map them into `ValidationDiagnostic` entries with line/column, severity `error`, and a short message. Add `getLineContext` utilities to return ±2 lines around an error for downstream display and copy payloads.
  </action>
  <verify>npm run typecheck</verify>
  <done>Parsing returns structured instructions plus diagnostics with line/column and context snippets.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` succeeds with grammar and parser modules.
</verification>

<success_criteria>
- SpinASM grammar provides tokens for highlightable syntax.
- Parser returns diagnostics with accurate line/column + context lines.
- Instruction list and symbol tables are available for analysis in later plans.
</success_criteria>

<output>
After completion, create `.planning/phases/01-phase-1/01-02-SUMMARY.md`
</output>
