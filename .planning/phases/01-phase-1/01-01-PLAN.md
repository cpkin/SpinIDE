---
phase: 01-phase-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.node.json
  - src/main.tsx
  - src/App.tsx
  - src/styles/app.css
  - src/diagnostics/types.ts
  - src/store/validationStore.ts
  - src/editor/editorExtensions.ts
  - src/editor/SpinEditor.tsx
  - src/ui/ResourceMeters.tsx
  - src/ui/DiagnosticsPanel.tsx
  - src/ui/CopyDiagnosticsButton.tsx
autonomous: true

must_haves:
  truths:
    - "User can type .spn code in a CodeMirror editor with line numbers"
    - "Validation panel and resource meters render in the UI shell"
    - "Copy button is visible and produces a placeholder payload"
  artifacts:
    - path: "src/editor/SpinEditor.tsx"
      provides: "CodeMirror editor wrapper"
    - path: "src/ui/DiagnosticsPanel.tsx"
      provides: "Diagnostics list UI"
    - path: "src/ui/ResourceMeters.tsx"
      provides: "Instruction/RAM/register meter UI"
    - path: "src/store/validationStore.ts"
      provides: "Shared validation state"
  key_links:
    - from: "src/App.tsx"
      to: "src/editor/SpinEditor.tsx"
      via: "React component composition"
      pattern: "<SpinEditor"
    - from: "src/App.tsx"
      to: "src/ui/DiagnosticsPanel.tsx"
      via: "React component composition"
      pattern: "<DiagnosticsPanel"
    - from: "src/App.tsx"
      to: "src/ui/ResourceMeters.tsx"
      via: "React component composition"
      pattern: "<ResourceMeters"
---

<objective>
Establish the React + Vite app scaffold and the validation UI shell (editor, meters, diagnostics list, copy button) so later plans can plug in real parsing and linting.

Purpose: Provide the base UI and state wiring required for validation feedback.
Output: Running React app with CodeMirror editor and placeholder diagnostics UI.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-phase-1/01-CONTEXT.md
@.planning/phases/01-phase-1/01-RESEARCH.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React app with dependencies</name>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.node.json, src/main.tsx, src/App.tsx, src/styles/app.css</files>
  <action>
    Initialize the Vite React + TypeScript app in the repo root (keep existing .planning, docs, tests). Install CodeMirror 6, Lezer, and Zustand dependencies per research. Create a minimal layout in `src/App.tsx` with header, resource meter row, editor area, diagnostics panel, and copy button. Add `src/styles/app.css` to provide a clean grid layout with spacing, readable monospace for the editor, and a subtle accent color for warning states.
  </action>
  <verify>npm run build</verify>
  <done>App builds successfully and shows the layout scaffold in the browser.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared diagnostics types and validation store</name>
  <files>src/diagnostics/types.ts, src/store/validationStore.ts</files>
  <action>
    Define `ValidationDiagnostic` and `ResourceUsage` types in `src/diagnostics/types.ts`, including severity (error|warning|info), message, optional suggestedFix, and line/column metadata. Create a Zustand store in `src/store/validationStore.ts` for source text, diagnostics list, and resource usage with setter actions. Seed the store with placeholder diagnostics/resources so the UI can render without real parsing yet.
  </action>
  <verify>npm run build</verify>
  <done>Store compiles with placeholder diagnostics accessible to UI components.</done>
</task>

<task type="auto">
  <name>Task 3: Build editor wrapper and UI shell components</name>
  <files>src/editor/editorExtensions.ts, src/editor/SpinEditor.tsx, src/ui/ResourceMeters.tsx, src/ui/DiagnosticsPanel.tsx, src/ui/CopyDiagnosticsButton.tsx, src/App.tsx</files>
  <action>
    Implement `SpinEditor` as a CodeMirror 6 wrapper that accepts `value` and `onChange`, uses `basicSetup`, line numbers, and a placeholder extension list from `editorExtensions.ts`. Build `ResourceMeters`, `DiagnosticsPanel`, and `CopyDiagnosticsButton` components that consume store data and render the placeholder diagnostics list, meter values, and a simple clipboard payload. Wire everything in `App.tsx` to the store with a readable empty-state hint in the editor area.
  </action>
  <verify>npm run build</verify>
  <done>Editor renders with line numbers and UI components render with placeholder data.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds and app loads with the UI scaffold.
</verification>

<success_criteria>
- CodeMirror editor renders with line numbers and editable content.
- Diagnostics panel and meters show placeholder values without runtime errors.
- Copy button writes a placeholder payload to the clipboard.
</success_criteria>

<output>
After completion, create `.planning/phases/01-phase-1/01-01-SUMMARY.md`
</output>
