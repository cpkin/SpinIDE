---
phase: 01-phase-1
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/analysis/resourceAnalyzer.ts
  - src/analysis/registerUsage.ts
  - src/analysis/lintRules.ts
  - src/analysis/analysisPipeline.ts
  - src/diagnostics/suggestions.ts
  - src/parser/ast.ts
  - src/parser/parseSpinAsm.ts
autonomous: true

must_haves:
  truths:
    - "Resource usage reports instruction count, delay RAM samples, and register usage"
    - "Seven lint rules emit WARNING diagnostics with actionable messages"
    - "Opcode typos surface suggested fixes"
  artifacts:
    - path: "src/analysis/resourceAnalyzer.ts"
      provides: "Instruction/RAM/register usage calculations"
    - path: "src/analysis/lintRules.ts"
      provides: "Lint rule definitions for LINT-01..07"
    - path: "src/analysis/analysisPipeline.ts"
      provides: "Unified validation output"
  key_links:
    - from: "src/analysis/analysisPipeline.ts"
      to: "src/parser/parseSpinAsm.ts"
      via: "ParseResult consumption"
      pattern: "parseSpinAsm"
    - from: "src/analysis/lintRules.ts"
      to: "src/diagnostics/suggestions.ts"
      via: "opcode suggestion helper"
      pattern: "suggestOpcode"
---

<objective>
Implement resource analysis and lint rule evaluation on top of the parser output, including suggestion helpers for opcode typos.

Purpose: Produce actionable warnings and resource usage data needed by the UI and copy payload.
Output: Analysis pipeline that returns lint diagnostics plus resource metrics.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-phase-1/01-CONTEXT.md
@.planning/phases/01-phase-1/01-RESEARCH.md
@docs/spinasm-spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compute resource usage metrics</name>
  <files>src/analysis/resourceAnalyzer.ts, src/analysis/registerUsage.ts, src/parser/ast.ts, src/parser/parseSpinAsm.ts</files>
  <action>
    Extend the parser output to capture instruction list, delay RAM allocations, and register references. Implement `resourceAnalyzer` to return instruction count (max 128), delay RAM usage in samples and milliseconds at 32 kHz (max 32768 samples), and register usage map. Add helpers in `registerUsage.ts` to track reads/writes per register (including equ-defined aliases).
  </action>
  <verify>npm run typecheck</verify>
  <done>Resource analyzer returns instruction count, delay RAM usage, and register usage list.</done>
</task>

<task type="auto">
  <name>Task 2: Implement lint rules with actionable warnings</name>
  <files>src/analysis/lintRules.ts, src/analysis/analysisPipeline.ts, src/diagnostics/suggestions.ts</files>
  <action>
    Implement LINT-01..07 as rule functions that inspect the parsed instruction list and resource data: unused pots, memory allocated but never written, memory written but never read, register read before write, output never written (dacl/dacr), potential clipping (gain sum > 1.0 without saturation), and delay address out of bounds. Create `suggestOpcode` helper using Levenshtein distance over known opcodes to produce `suggestedFix` text for unknown opcodes. Build `analysisPipeline` that merges parse diagnostics with lint warnings and returns `{ diagnostics, resources }`.
  </action>
  <verify>npm run typecheck</verify>
  <done>Lint diagnostics are emitted as WARNING severity with messages and optional suggested fixes.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` succeeds with analysis modules.
</verification>

<success_criteria>
- Resource metrics reflect instruction count, delay RAM usage, and register usage.
- Seven lint warnings produce actionable messages.
- Unknown opcodes include a suggested fix where possible.
</success_criteria>

<output>
After completion, create `.planning/phases/01-phase-1/01-03-SUMMARY.md`
</output>
