---
phase: 00-foundation-test-infrastructure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/corpus/official/*.spn
  - tests/corpus/community/*.spn
  - tests/corpus/corpus.json
  - docs/community-gaps.md
autonomous: true

must_haves:
  truths:
    - "Test corpus contains 20-30 .spn programs with expected resource checks."
    - "Community tooling gaps are documented from at least two forums."
  artifacts:
    - path: "tests/corpus/official"
      provides: "Official demo SpinASM programs"
    - path: "tests/corpus/community"
      provides: "Vetted community SpinASM programs"
    - path: "tests/corpus/corpus.json"
      provides: "Manifest with expected resource checks"
    - path: "docs/community-gaps.md"
      provides: "Summary of pain points from forums"
  key_links:
    - from: "tests/corpus/corpus.json"
      to: "tests/corpus/official"
      via: "spnPath entries"
      pattern: "tests/corpus/official"
    - from: "tests/corpus/corpus.json"
      to: "tests/corpus/community"
      via: "spnPath entries"
      pattern: "tests/corpus/community"
---

<objective>
Assemble a validation corpus of official and community SpinASM programs and document community tooling gaps.

Purpose: Provide parser regression fixtures and capture real-world pain points before implementation.
Output: Corpus directories, manifest with expected resource checks, and community gap summary.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-foundation-test-infrastructure/00-CONTEXT.md
@.planning/phases/00-foundation-test-infrastructure/00-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build SpinASM test corpus and manifest</name>
  <files>tests/corpus/official/*.spn, tests/corpus/community/*.spn, tests/corpus/corpus.json</files>
  <action>
Create corpus directories and populate them with 20-30 `.spn` programs:
- 10+ official demos (Spin Semiconductor demos or official examples)
- 10+ community examples (fv1experiments, SpinCAD exports, or asfv1 examples)
Store raw `.spn` files in `tests/corpus/official` and `tests/corpus/community` with stable filenames.

Create `tests/corpus/corpus.json` manifest with entries:
- `id`, `source` (official/community), `spnPath`
- `expect.instructions` (integer, <=128)
- `expect.ramSamples` (integer, <=32768)
- `expect.registers` (array of register names used)

Use `pip install asfv1` (or a similar assembler tool) to assemble each file and capture instruction count, RAM usage, and registers. Record the values in the manifest. If a file cannot be assembled, replace it or document why in the manifest `notes` field (do not leave expectations empty).
  </action>
  <verify>
`tests/corpus/corpus.json` lists 20-30 entries and every `spnPath` file exists on disk.
  </verify>
  <done>
Corpus directories contain official and community programs with a complete manifest of expected resources.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document community tooling gaps</name>
  <files>docs/community-gaps.md</files>
  <action>
Create `docs/community-gaps.md` summarizing top tooling pain points. Requirements:
- Source at least two forums (diystompboxes.com and PedalPCB)
- Capture at least 3 distinct pain points (e.g., lack of simulator, Windows-only tooling)
- Include links to specific threads and short quotes/snippets (no full text dumps)
- Summarize implications for SpinGPT in a final section
  </action>
  <verify>
Doc contains two forum sources with links and three summarized pain points.
  </verify>
  <done>
Community feedback is captured with sources and concrete gaps.
  </done>
</task>

</tasks>

<verification>
Corpus size meets target and manifest provides resource expectations for all entries.
</verification>

<success_criteria>
- 20-30 `.spn` files exist across official and community corpus folders.
- Corpus manifest provides expected instruction count, RAM usage, and registers for each file.
- Community gaps doc cites at least two forums with three pain points.
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation-test-infrastructure/00-03-SUMMARY.md`
</output>
