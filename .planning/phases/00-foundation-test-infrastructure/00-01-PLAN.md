---
phase: 00-foundation-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/spinasm-spec.md
  - docs/simulator-strategy.md
  - docs/simulation-fidelity.md
autonomous: true

must_haves:
  truths:
    - "Dialect specification defines supported directives/opcodes and parser behavior."
    - "Simulator strategy and fidelity target are documented for future implementation."
  artifacts:
    - path: "docs/spinasm-spec.md"
      provides: "SpinASM dialect specification with syntax and examples"
    - path: "docs/simulator-strategy.md"
      provides: "Simulation approach, reference implementations, and validation plan"
    - path: "docs/simulation-fidelity.md"
      provides: "Fidelity target definition and acceptable deviations"
  key_links:
    - from: "docs/spinasm-spec.md"
      to: "http://www.spinsemi.com/Products/datasheets/spn1001-dev/SPINAsmUserManual.pdf"
      via: "reference link"
      pattern: "SPINAsmUserManual"
    - from: "docs/simulator-strategy.md"
      to: "docs/simulation-fidelity.md"
      via: "explicit link"
      pattern: "simulation-fidelity"
---

<objective>
Lock the SpinASM dialect specification and document simulator strategy plus fidelity targets.

Purpose: Establish authoritative parser and simulation references before implementation.
Output: Spec and strategy docs with clear scope, examples, and fidelity constraints.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-foundation-test-infrastructure/00-CONTEXT.md
@.planning/phases/00-foundation-test-infrastructure/00-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft SpinASM dialect specification</name>
  <files>docs/spinasm-spec.md</files>
  <action>
Create `docs/spinasm-spec.md` as the authoritative dialect reference. Use the SPINAsm User Manual as canonical and cite it. Include sections:
- Scope (official SpinASM only; core directives only: equ, mem, org)
- Lexical rules (case-insensitive mnemonics/labels, semicolon comments only, whitespace rules, line endings)
- Directive syntax/semantics (equ/mem/org with examples)
- Label rules (format, reuse rules, forward references)
- Instruction set listing (name + operand patterns; note any operand constraints)
- Error handling policy (unknown opcodes/directives warn and continue)
- Examples of valid programs (at least 3, include labels and comments)
Avoid speculative rules; if ambiguous, add a "Needs confirmation" note and link the manual section.
  </action>
  <verify>
Confirm `docs/spinasm-spec.md` contains the required sections and references the official manual link.
  </verify>
  <done>
Spec doc exists with explicit directive rules, instruction list, and parser behavior notes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document simulator strategy and fidelity target</name>
  <files>docs/simulator-strategy.md, docs/simulation-fidelity.md</files>
  <action>
Create `docs/simulator-strategy.md` describing the simulation approach. Include:
- Reference implementations to consult (SpinCAD, asfv1, official manual)
- Fixed-point math approach (1.23 scaling, saturation/overflow expectations)
- 32-sample block processing requirement and pot update cadence
- 32 kHz native sample rate and resampling strategy for inputs
- Validation approach using the Phase 0 corpus
- Performance target (<2s for 30s render on modern hardware)

Create `docs/simulation-fidelity.md` defining the "gross correctness" target. Include:
- What must match hardware (instruction semantics, delay lengths, resource limits)
- Acceptable deviations (no cycle-accurate timing, minor spectral differences)
- Known risks to document in UI later
- Pass/fail criteria for corpus comparisons
Link from simulator strategy doc to fidelity doc.
  </action>
  <verify>
Both docs exist and `docs/simulator-strategy.md` references `docs/simulation-fidelity.md` by path.
  </verify>
  <done>
Simulation approach and fidelity target are documented with concrete constraints.
  </done>
</task>

</tasks>

<verification>
Docs are present and align with Phase 0 decisions (official SpinASM scope, strict metadata policy, warning-on-unknown).
</verification>

<success_criteria>
- `docs/spinasm-spec.md` captures parser target rules with examples.
- `docs/simulator-strategy.md` and `docs/simulation-fidelity.md` define implementation constraints and fidelity boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation-test-infrastructure/00-01-SUMMARY.md`
</output>
